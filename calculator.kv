<GreenButton@Button>:
    background_color: 0, 0, 0, 0
    color: 0, 0, 0, 1
    font_size: 24
    bold: True
    canvas.before:
        Color:
            rgba: 0, 0.3, 0, 0.6
        RoundedRectangle:
            pos: self.pos
            size: self.size
            radius: [10,]

        Color:
            rgba: 0, 0, 0, 0.7
        Line:
            rounded_rectangle: [self.x, self.y, self.width, self.height, 10]
            width: 1.5

<CalculatorLayout>:
    orientation: 'vertical'
    spacing: 30
    padding: 5

    canvas.before:
        Rectangle:
            pos: self.pos
            size: self.size
            source: 'assets/images/background.jpeg'


    TextInput:
        id: display
        text: '0'
        font_size: 40
        size_hint_y: 0.3
        multiline: False
        readonly: True
        background_color: 0, 0, 0, 0.5
        foreground_color: 0, 1, 0, 1
        cursor_color: 0, 1, 0, 1

    GridLayout:
        cols: 4
        rows: 5
        spacing: 30


        GreenButton:
            text: '7'
            on_press:
                app.root.digit_button('7')
                app.root.play_click_sound()
        GreenButton:
            text: '8'
            on_press:
                app.root.digit_button('8')
                app.root.play_click_sound()
        GreenButton:
            text: '9'
            on_press:
                app.root.digit_button('9')
                app.root.play_click_sound()
        GreenButton:
            text: '/'
            on_press:
                app.root.operation_button('/')
                app.root.play_click_sound()

        GreenButton:
            text: '4'
            on_press:
                app.root.digit_button('4')
                app.root.play_click_sound()
        GreenButton:
            text: '5'
            on_press:
                app.root.digit_button('5')
                app.root.play_click_sound()
        GreenButton:
            text: '6'
            on_press:
                app.root.digit_button('6')
                app.root.play_click_sound()
        GreenButton:
            text: '*'
            on_press:
                app.root.operation_button('*')
                app.root.play_click_sound()

        GreenButton:
            text: '1'
            on_press:
                app.root.digit_button('1')
                app.root.play_click_sound()
        GreenButton:
            text: '2'
            on_press:
                app.root.digit_button('2')
                app.root.play_click_sound()
        GreenButton:
            text: '3'
            on_press:
                app.root.digit_button('3')
                app.root.play_click_sound()
        GreenButton:
            text: '-'
            on_press:
                app.root.operation_button('-')
                app.root.play_click_sound()

        GreenButton:
            text: '0'
            on_press:
                app.root.digit_button('0')
                app.root.play_click_sound()
        GreenButton:
            text: '.'
            on_press:
                app.root.decimal_button()
                app.root.play_click_sound()
        GreenButton:
            text: 'C'
            on_press:
                app.root.clear_button()
                app.root.play_click_sound()
        GreenButton:
            text: '+'
            on_press:
                app.root.operation_button('+')
                app.root.play_click_sound()

    GreenButton:
        text: '='
        on_press:
            app.root.equals_button()
            app.root.play_click_sound()
        size_hint_y: 0.2
        font_size: 30


<ErrorPopup>:
    title: 'MATH ERROR'
    size_hint: 0.8, 0.6
    BoxLayout:
        orientation: 'vertical'
        Label:
            text: 'ARE YOU FUCKING KIDDING ME!? 0?? REALLY?? YOU MOTHERFUCKER, HOW MORE STUPID YOU CAN BE, DUMBASS!? GO BACK TO SCHOOL, YOU STINKY KID, BRUH. SAY THANKS THAT I DIDN`T DOWNLOAD A VIRUS ONTO YOUR COMPUTER, DICKHEAD -_-'
            text_size: self.width, None
            halign: 'center'
            valign: 'middle'
            size_hint_y: 0.8
        Button:
            text: 'OK'
            size_hint_y: 0.2
            on_press:
                root.dismiss()
                app.root.stop_error_sound(None)

